name: Guardrails Diagnostic

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  startup-check:
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - name: Confirm runner startup
        run: echo "runner-started"
